# ti 开发约定

给读代码和改代码的人（含 agent）看。需求与设计仍以 `docs/PRD.md`、`docs/DESIGN.md` 为准。

## 处女原则

改代码、加功能和收尾优化，都要像第一次写成这样，不要留打补丁的痕迹。

- 改完后通读相关函数和文件：命名、结构、注释应是最终形态，看不出「先写成 A 再改成 B」
- 不要用将就的变量名、废弃的函数、注释掉的旧逻辑、只为迁就旧调用的多余分支
- 删掉的行为要连定义、调用、启动文案一起拿掉，不要留空壳
- 能重写成一条干净路径就重写，不要在旧路径上叠 if

## 命名

- 函数和变量见名知意。读名字就知道方向和对象，不要让人猜「to 的是哪边」。
- 协议边界尤其要写清：内部类型还是线上格式。例如 `toWire` 是内部 → 各家 API；`toInternalAssistant` 是流碎片 → 内部消息。

## 注释

- 只用 `//`。不要 `/** */` 或 `/* */`
- 注释末尾不要句号
- 注释讲通用逻辑、功能、语法。不要写会过期的清单或「仅某某」这种和当前数据强绑定的话。厂家名、模型名写在数据里，不写进注释
- 注释里不要用 `/` 表示「或」或并列，写成「或」或顿号，以免和 `//` 混在一起。命令名 `/model` 这种可以保留
- 每个函数（含内部函数、导出函数）正上方只介绍这个函数整体是干什么的。不要复述函数名，不要写按键、返回值、分支这些细节（看类型和行内注释）
- 复杂的地方（协议边界、中断与队列、配置合并、重绘、状态机）在关键步骤旁加注释，讲「为什么」和容易踩的分支
- 一眼能看懂的赋值、转发、简单 getter 不必再写行内注释
- 标识符用英文；注释用中文
