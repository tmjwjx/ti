# ti 开发约定

给读代码和改代码的人（含 agent）看。需求与设计仍以 `docs/PRD.md`、`docs/DESIGN.md` 为准。

## 命名

- 函数和变量见名知意。读名字就知道方向和对象，不要让人猜「to 的是哪边」。
- 协议边界尤其要写清：内部类型还是线上格式。例如 `toWire` 是内部 → 各家 API；`toInternalAssistant` 是流碎片 → 内部消息。

## 注释

- 只用 `//`。不要 `/** */` 或 `/* */`。
- 注释里不要用 `/` 表示「或」或并列，写成「或」或顿号，以免和 `//` 混在一起。命令名 `/model` 这种可以保留。
- 每个函数（含内部函数、导出函数）正上方写一两句中文：做什么、关键逻辑怎么走。不要复述函数名。
- 复杂的地方（协议边界、中断与队列、配置合并、重绘、状态机）在关键步骤旁加注释，讲「为什么」和容易踩的分支。
- 一眼能看懂的赋值、转发、简单 getter 不必再写行内注释。
- 标识符用英文；注释用中文。
